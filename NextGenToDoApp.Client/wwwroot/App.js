const set_document_title = (title) => { document.title = title };
const log = (text) => { console.log(text) };
window.set_document_title("Next Gen To-Do App")
const create_UI = (node) => { document.body.appendChild(node) };
const h1 = (text) => { const elem = document.createElement('h1');
		for (const child of children) {
			elem.appendChild(child);
		}
		return elem; };
const txt = (text) => { document.createTextNode(text) };
